class Solution:
    def minValue(self, a, b, n):
        
        a.sort()
        b.sort(reverse=True)
        summ = 0
        i = 0
        while i < n:
            summ += (a[i]*b[i])
            i += 1
        return summ
